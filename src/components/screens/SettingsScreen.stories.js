import React from 'react';
import {storiesOf} from '@storybook/react-native';

import {SettingsScreen} from './SettingsScreen';

storiesOf('screens', module).add('SettingsScreen', () => <SettingsScreen />);
