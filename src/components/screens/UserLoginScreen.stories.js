import React from 'react';
import {storiesOf} from '@storybook/react-native';

import {UserLoginScreen} from './UserLoginScreen';

storiesOf('screens', module).add('UserLoginScreen', () => <UserLoginScreen />);
