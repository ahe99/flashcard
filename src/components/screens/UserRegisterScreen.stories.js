import React from 'react';
import {storiesOf} from '@storybook/react-native';

import {UserRegisterScreen} from './UserRegisterScreen';

storiesOf('screens', module).add('UserRegisterScreen', () => (
  <UserRegisterScreen />
));
